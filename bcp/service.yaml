apiVersion: v1
kind: Service
metadata:
  name: {{ include "porfolio.fullname" . }}
  labels:
    {{- include "porfolio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "porfolio.selectorLabels" . | nindent 4 }}
